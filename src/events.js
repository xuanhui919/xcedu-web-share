export const CHANGE_THEME = 'xcedu:changeTheme'


const registerEvent = () => {
  window.addEventListener(CHANGE_THEME, event => {

  })
}

const dispathChangeThemeEvent = (event, data) => {

}


export { registerEvent, dispathEvent }
